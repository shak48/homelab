- name: Bootstrap PVE Main hosts
  hosts: all
  become: true

  roles:
    - common-role
    - bootstrap
    - harden



# For new server's this seems to bootstrap ansible user well. No need to manual copy keys, \ 
# assuming root log in is still enabled.
  # ansible-playbook playbooks/common/bootstrap-all.yml -u root -l pbs2 -kk --check