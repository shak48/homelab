- name: Run bootstrap tasks 
  include_tasks: "{{ item }}"
  loop:
    - set-timezone.yml
    - install-bootstrap-packages.yml
    - create-authorized-users.yml
    - install-authorized-keys.yml
    - passwordless-sudo.yml
    - enable-fallback-tty-console.yml
